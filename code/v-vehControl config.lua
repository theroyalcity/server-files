Config = {
    Debug = true,
    CommandName = "vehmenu",
    Keybind = {
        enabled = true,
        key = "B" -- The Key to opening the menu if enabled
  },
  Functions = {
      HasKeys = function()
          return true
    end
  }
